// Skeleton tests for Plugins page (#7320)
// Requires vitest + @testing-library/react to run.

import { describe, it, expect } from "vitest";

describe("Plugins page", () => {
  it.todo("renders plugin cards when API returns data");
  it.todo("shows loading spinner while fetching");
  it.todo("disables the grid when plugins_enabled is false");
  it.todo("renders capability badges for each plugin");
  it.todo("displays loaded/unloaded status correctly");
});
