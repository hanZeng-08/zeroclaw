// Skeleton tests for McpDashboard page (#7320)
// Requires vitest + @testing-library/react to run.

import { describe, it, expect } from "vitest";

describe("McpDashboard page", () => {
  it.todo("renders server cards with enabled/disabled badges");
  it.todo("lists tools exposed by each server");
  it.todo("renders bundle cards with bound server names");
  it.todo("shows empty state when no servers or bundles are configured");
  it.todo("gracefully handles API errors");
});
